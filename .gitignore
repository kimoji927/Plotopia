# ============================================================
# Unreal Engine 项目 .gitignore
# 适用版本: UE 5.7 (Plotopia)
# ============================================================

# ---------- UE 自动生成/缓存目录（绝不提交） ----------
# 编译产物（DLL/obj 等，重新编译会重新生成）
Binaries/
**/Binaries/

# 构建中间文件（UBT/UHT 生成，体积巨大）
Intermediate/
**/Intermediate/

# 派生数据缓存（着色器/烘焙缓存，可达几十 GB）
DerivedDataCache/
**/DerivedDataCache/

# 编辑器本地状态（日志、配置、布局）
Saved/
**/Saved/

# ---------- IDE 配置（本地个人配置，不提交） ----------
.vs/
.idea/
.vscode/
*.suo
*.user
*.userprefs
*.DotSettings.user

# ---------- Windows 系统文件 ----------
Thumbs.db
Desktop.ini
ehthumbs.db
*.pdb
*.ipdb
*.iobj

# ---------- 日志与临时文件 ----------
*.log
*.tmp
*.bak
*.orig

# ---------- 第三方资产（不提交，体积巨大且可自行获取） ----------
# 这些目录属于第三方/Epic 提供的资产（合计约 10GB）：
# - ParagonBoris / ParagonMinions: Epic 免费角色
# - Stylized_Spruce_Forest: Epic 免费 PCG 森林场景
# - Fantastic_Village_Pack: Epic 免费奇幻村庄资产
# - NiagaraExamples: 引擎自带 Niagara 示例
# - Medieval_MWP: 第三方中世纪建筑资产包（贴图单文件约 88MB）
# 克隆后如需完整运行，请自行获取同名资产
Content/ParagonBoris/
Content/ParagonMinions/
Content/Stylized_Spruce_Forest/
Content/NiagaraExamples/
Content/Fantastic_Village_Pack/
Content/Medieval_MWP/

# ---------- UE OFPA 外部 Actor 存储（体积不可控，不提交） ----------
# __ExternalActors__ 是 UE5 的 "One File Per Actor" 存储目录，由编辑器自动生成。
# ⚠️ 本项目该目录体积不可控：kaifang 关卡内的 PCGWorldActor 会序列化地形缓存
#    （LandscapeCache / EPCGLandscapeCacheSerializationMode），
#    曾两次膨胀至单文件 189MB，超过 GitHub 的 100MB 单文件限制。
#    虽可在编辑器中关闭序列化（关闭后 221MB -> 33MB），但重建/重存关卡后会再次生成，
#    因此本项目选择整体排除该目录；关卡效果通过 Screenshots/ 与演示视频展示。
Content/__ExternalActors__/
Content/__ExternalObjects__/

# ---------- 截图原始素材（高分辨率原图，不提交） ----------
# Screenshots/ 下提交的是压缩后的 .jpg（用于 README 展示，总计约 2MB）；
# 原始高分辨率 PNG 保留在本地 _original_png/ 供后续使用，不入库。
Screenshots/_original_png/

# ---------- 保持提交的文件（显式例外） ----------
# .uproject / Config / Source / Content/GAS 是需要入库的核心内容（约 25MB）
# Content/GAS 为自创资产，全部入库；第三方资产见上方排除规则
